run:
  timeout: 3m

linters:
  enable:
    - gofmt
    - govet
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
